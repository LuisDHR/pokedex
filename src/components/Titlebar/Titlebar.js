// Componente que muestra el título del proyecto (Pokédex)
// en una barra situada en la parte superior de la página.

import './style.css'

const Titlebar = () => {
    return (
        <div className="main-title">
            Pokédex
        </div>
    );
}

export default Titlebar
